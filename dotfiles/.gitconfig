[user]
	name = Bryan Berry
	email = berr1167@gmail.com
[alias]
	co = checkout
	ci = commit
	st = status
	br = branch
	hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
	type = cat-file -t
	dump = cat-file -p
	prom = pull --rebase origin master
	pom = push origin master
